DATABASE_URL="file:./dev.db"

# Auth.js — generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
AUTH_SECRET=""

# Resend (magic link + transactional email)
RESEND_API_KEY=""
EMAIL_FROM="Bedlay Gardens <onboarding@resend.dev>"
